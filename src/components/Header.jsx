import "../scss/header.scss"

const Header = () => {
  return (
    <div className="Header">
      <span>About Me</span>
      <span>Proyects</span>
      <span>Contact</span>
    </div>
  );
}

export default Header;
